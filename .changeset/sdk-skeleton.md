---
'usdh-kit': minor
---

Initial SDK skeleton. Public types (Signer, KitConfig, SwapInput, SwapResult, Quote, Logger), typed error hierarchy rooted at `UsdhKitError`, and `createUsdhKit()` factory with input validation. Execution lands in follow-up PRs.
